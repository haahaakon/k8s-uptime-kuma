apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: uptime-kuma
  namespace: kuma
  annotations:
     external-dns.alpha.kubernetes.io/hostname: uptime.br0.no
spec:
  parentRefs:
    - name: kuma-gw
  hostnames:
    - "uptime.br0.no"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: uptime-kuma-service
          port: 3001
